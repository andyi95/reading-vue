version: '3.4'

services:
  nginx:
    ports:
      - 80:80

  frontend:
#    volumes:
#      - node_modules:/app/node_modules
#      - ./frontend:/app:cached
    ports:
      - 4000:4000

  backend:
    volumes:
      - ./backend:/app:cached
    ports:
      - 8000:8000


volumes:
  node_modules:
